const path = require('path');
require('../../../../uploads/backups')

// REFACTOR: STILL TENTATIVE
async function loadBackup() {
	// try {
	// 	const upload_path = path.resolve(__dirname, `../../../../uploads/backups/${process.env.DB_NAME}`);


	// } catch (error) {
		
	// }
}

// module.exports = loadBackup;